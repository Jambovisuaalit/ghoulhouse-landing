name: Release preview smoke (temporary)

on:
  push:
    branches: [qa/release-6c5538]

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      QA_BASE_URL: https://ghoulhouse-v5-runtime-qa.vercel.app
      QA_SCREENSHOT_DIR: qa-artifacts/release-preview
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Preview HTTP SEO security and image-format preflight
        shell: bash
        run: |
          set -euo pipefail
          echo 'check: production-oriented metadata + QA host noindex safety'
          html="$(curl --fail --silent "$QA_BASE_URL/")"
          grep -q '<title>GhoulHouse | Työmaakuvat sisään. Valmis some ulos.</title>' <<<"$html"
          grep -q 'rel="canonical" href="https://ghoulhouse.fi"' <<<"$html"
          grep -q '<meta name="robots" content="index, follow"' <<<"$html"
          grep -q 'property="og:title"' <<<"$html"
          grep -q 'twitter:card' <<<"$html"
          grep -q 'application/ld+json' <<<"$html"
          grep -q '"url":"https://ghoulhouse.fi"' <<<"$html"
          if grep -Eq 'rel="canonical" href="https://ghoulhouse-v5-runtime|"url":"https://ghoulhouse-v5-runtime' <<<"$html"; then
            echo '::error::QA runtime URL leaked into canonical or structured data.'
            exit 1
          fi

          echo 'check: QA safety and security headers'
          headers="$(mktemp)"
          curl --silent --head "$QA_BASE_URL/" > "$headers"
          grep -qi '^x-robots-tag:.*noindex' "$headers"
          grep -qi '^x-content-type-options: nosniff' "$headers"
          grep -qi '^x-frame-options: DENY' "$headers"
          grep -qi '^referrer-policy: strict-origin-when-cross-origin' "$headers"
          grep -qi '^permissions-policy:' "$headers"
          grep -qi '^cross-origin-opener-policy: same-origin' "$headers"
          grep -qi '^content-security-policy:' "$headers"

          echo 'check: responsive image negotiation'
          image_path='/_next/image?url=https%3A%2F%2Fimages.unsplash.com%2Fphoto-1768321917661-d4f1a89d2185%3Fauto%3Dformat%26fit%3Dcrop%26q%3D86%26w%3D1800&w=640&q=75'
          avif_headers="$(mktemp)"
          webp_headers="$(mktemp)"
          curl --fail --silent --output /dev/null --dump-header "$avif_headers" -H 'Accept: image/avif' "$QA_BASE_URL$image_path"
          curl --fail --silent --output /dev/null --dump-header "$webp_headers" -H 'Accept: image/webp' "$QA_BASE_URL$image_path"
          grep -qi '^content-type: image/avif' "$avif_headers"
          grep -qi '^content-type: image/webp' "$webp_headers"

          echo 'check: lead validation API'
          invalid_status="$(curl --silent --output /tmp/lead-invalid.json --write-out '%{http_code}' \
            --request POST --header 'content-type: application/json' --data '{}' "$QA_BASE_URL/api/leads")"
          test "$invalid_status" = '400'
          grep -q '"validation_error"' /tmp/lead-invalid.json

      - name: Canonical browser smoke
        run: node scripts/browser-qa-ci.mjs

      - name: Visual and accessibility smoke
        run: node scripts/launch-visual-a11y-qa.mjs

      - name: No-JS smoke
        run: node scripts/no-js-qa-ci.mjs

      - name: Boundary navigation form and reduced-motion smoke
        run: node scripts/release-preview-smoke-temp.mjs

      - name: Upload release smoke artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: release-preview-smoke
          path: qa-artifacts/release-preview/
          if-no-files-found: warn
          retention-days: 2
